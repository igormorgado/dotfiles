[user]
	email = morgado.igor@gmail.com
	name = Igor Morgado
[core]
	excludesfile = /home/igormorgado/.gitignore
[credential]
	helper = store --file ~/.git-credentials
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
