{{- $home := .chezmoi.homeDir -}}
{{- $hostname := .chezmoi.hostname -}}

[edit]
command = "nvim"

[diff]
command = "delta"
exclude = ["scripts"]

[git]
    autoCommit = true
    commitMessageTemplate = "{{ promptString \"Commit message\" }}"
